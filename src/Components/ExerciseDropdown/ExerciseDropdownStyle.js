import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderRadius: 50,
    borderColor: "#ccc",
    marginBottom: 16,
    width: '80%',
  },
  loaderContainer: {
    paddingVertical: 20,
  },
});