import { Button, StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    borderWidth: 1,
    borderRadius: 8,
    borderColor: "#ccc",
    marginBottom: 16,
    paddingTop: 30,
  },
  button_spacing: {
    padding: 12,
    borderRadius: 8,
    marginVertical: 5, 
    marginHorizontal: 50, 
  },
});