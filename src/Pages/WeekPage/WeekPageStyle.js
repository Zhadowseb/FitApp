import { StyleSheet } from 'react-native';

export default StyleSheet.create({

    container: {
        flex: 1,
    },

    header: {
        flex: 0.1,
        alignItems: 'center',
        justifyContent: 'center',
    },

    body: {
        flex: 0.9,
        paddingBottom: 40,
    },
});